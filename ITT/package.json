{
  "name": "tradex-platform",
  "version": "1.0.0",
  "description": "Full-stack trading website",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend --legacy-peer-deps",
    "build": "npm run install:all && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
